<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

	<parent>
        <groupId>com.elster.jupiter</groupId>
        <artifactId>parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>bpm.extjs</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>bundle</packaging>
    <name>[Jupiter] com.elster.jupiter.bpm.extjs</name>

    <dependencies>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.compendium</artifactId>
        </dependency>
        <dependency>
            <groupId>com.elster.jupiter</groupId>
            <artifactId>cbo</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.elster.jupiter</groupId>
            <artifactId>http.whiteboard</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>sencha-build</id>
            <properties>
                <js.dir>js/bpm</js.dir>
            </properties>
            <!--build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>1.7</version>
                        <executions>
                            <execution>
                                <id>sencha-compile</id>
                                <phase>generate-resources</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target unless="skipSencha">
                                        <echo>Sencha build</echo>
                                        <echo>Using Sencha CMD in ${sencha.cmd.dir}</echo>
                                        <echo>Using Ext lib in ${sencha.ext.dir}</echo>
                                        <property name="build.dir" value="${project.build.directory}/web"/>
                                        <property name="build.temp.dir" value="${project.build.directory}/webtemp"/>
                                        <property name="cmd.dir" value="${sencha.cmd.dir}"/>
                                        <property name="ext.dir" value="${sencha.ext.dir}"/>
                                        <ant antfile="build.xml" target="build" dir="./src/main/web/js/usr/"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.felix</groupId>
                        <artifactId>maven-bundle-plugin</artifactId>
                        <configuration>
                            <instructions>
                                <Include-Resource>{maven-resources}</Include-Resource>
                            </instructions>
                        </configuration>
                    </plugin>
                </plugins>
                <resources>
                    <resource>
                        <directory>${basedir}/target/web</directory>
                        <targetPath>js/usr</targetPath>
                        <includes>
                            <include>*.html</include>
                            <include>*.js</include>
                            <include>resources/**/*</include>
                        </includes>
                    </resource>
                </resources>
            </build!-->
        </profile>

    </profiles>
</project>