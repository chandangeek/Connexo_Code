Ext.define('Mdc.dynamicprivileges.Stores', {
    singleton: true,

    required: [
        'Mdc.store.DeviceStatePrivileges'
    ],

    deviceStateStore: [
        'Mdc.store.DeviceStatePrivileges'
    ]
});
